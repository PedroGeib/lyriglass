{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the overlay and settings windows",
  "windows": ["overlay", "settings"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
